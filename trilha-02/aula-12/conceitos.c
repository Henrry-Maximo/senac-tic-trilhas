#include <stdio.h>

int main() {
  // Expressão = Tem Um Valor ( ??? )
  // (2 + 2) ===> 4
  // (media >= 7.4) ====> 1 ou 0 
  // (vendas > 1000.0) ? 500.0 : 100.0 ===> 500.0 ou 100.0

  // ----------------------------------------------------
  // Comando = Faz Uma Ação ( ??? )
  int x = 10; // comando que declara uma variável e guarda um valor nela
  printf("Olá, mundo"); // comando que manda o computador imprimir algo na tela

  if (x > 10) {
    // segue caminho A
  } else {
    // segue caminho B
  }

  return 0;
}